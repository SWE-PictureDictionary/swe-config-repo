server:
  port: 8080

spring:
  application:
    name: progress-manager

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

content:
  access:
    url: ${CONTENT_ACCESS_URL:http://content-access}
  manager:
    url: ${CONTENT_MANAGER_URL:http://content-manager}

progress:
  access:
    url: ${PROGRESS_ACCESS_URL:http://progress-access}

completion:
  engine:
    url: ${COMPLETION_ENGINE_URL:http://completion-engine}